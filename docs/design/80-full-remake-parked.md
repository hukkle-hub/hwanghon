# 80 — 아인·세라 전신 재제작 (보류) · 쓸 수 있게 남긴 도구

디렉터 결정(2026-09-25): 「맨몸 기본 에셋으로 전투를 먼저 다 만들고, 옷·체형·얼굴·머리는 나중에 입힌다」.
외형을 바꿀 때마다 무게·쥔 손·장비 자리·낫 궤적·시험 기준이 줄줄이 흔들려 전투 작업이 멈췄다 → **게임 모델은 배포된 것 그대로 두고**, 재제작본은 원본으로만 보관한다.

## 보관한 것
- `art/3d/src/ain_body_v80.glb`, `art/3d/src/sera_body_v80.glb` — Tripo 전신(설정 시트) + Blender 무게 + 쥔 손 좌표·장비 자리 적용본. 게임에서 읽지 않는다.
- 남은 문제: 세라 이마의 어두운 얼룩(Tripo 원 텍스처의 머리칼 그림자 — 얼굴 투영 타원을 이마까지 넓혀 다시 구우면 된다, 파란 타원 1024,1000,430,660).

## 나중에 «옷 입히기» 때 쓸 도구 (모두 확대 계측으로 검증)
| 도구 | 하는 일 | 잰 값 |
|---|---|---|
| `tools/3d/blender-autoweight.py` | Blender 뼈 열 확산 무게 + 팔 가리기(`--arm-mask`) · 목 띠 이웃 평균(`--region-smooth`) · 치맛단 골반 쪽(`--skirt`) · 머리 한 몸 · 6만 삼각형으로 줄이기(`--target`, 머리·손 보존) | 늘어난 변 비율(17 클립, stretch-view) 아인 150→112‰ · 세라 384→192‰ — 그 뒤 띠 보정으로 아인 나쁜 변 73.7k→40.4k(28 클립, stretch-map) |
| `tools/3d/stretch-map.mjs` | 늘어남을 «뼈 쌍» 별로 세어 어디가 문제인지 알려 준다 | — |
| `tools/3d/ain-grip-frame.py` | 새 손을 옛 손 좌표로 겹치는 변환(ICP) → 검증된 «쥔 손» 을 그대로 쓴다 | 겹친 60 % 거리 6.8 / 7.6 mm |
| `tools/3d/gear-anchor.mjs` | 옛 몸의 장비 자리를 새 뼈에 `gearAnchor` 로 | 후드가 어깨로 흘러내리던 것 해결 |
| `tools/3d/tex-pad.py` | 얼굴 조각 UV 패딩 | — |
| `tools/3d/gear-view.html` | 방어구·장신구 맞춤 보기 | — |

## 지금 게임에 들어간 코드 개선 (옛 모델로 시험 357/357)
- `js/ain-grip-ik.js`: 팔이 펴질 때 손을 당기는 보정을 90 %에서 «딱» 켜지던 것 → C1 곡선(86~104 %). 궁극기 끝 오른 아래팔 8.1°/표본 튐 제거.
- `js/ain-two-hand.js`: 스킬4 세운 키 .92 → .88 (끝에서 8.2° 튐 → 7° 아래). 경로 기준 어깨(AUTHORED_SHOULDERS) — 몸이 바뀌어도 낫이 짠 자리로.
- `js/ain-bind-repair.js`·`js/ain-grip-shape.js`: 새 메시(meshSwap)면 옛 보정은 건너뛰고 쥔 손만(gripFrame·gripCurl).
- `js/looks.js`·`js/portrait3d.js`: `gearAnchor` 우선.
- 시험: 바람(몸통·팔 정점 0 개로 직접), 스킬3 판정 손 속도(최고속의 ¼ + 6 m/s), 두 손(새 메시 분기).
