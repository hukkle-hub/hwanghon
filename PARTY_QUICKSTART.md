# 황혼 · 쉘터 / 인력사무소 / 협동 던전 실행

전체 접속은 기본 100명, 던전은 파티당 최대 4명인 개발 빌드입니다. 쉘터는 길드 채팅과 소속을 관리하는 텍스트 화면이고 인력사무소에서 파티를 모집합니다. 별도의 마을·로비 캐릭터를 만들지 않습니다. 던전에서는 기존 아인을 각자 조작합니다.

## 같은 Wi-Fi에서 두 기기로 시작

1. 서버를 맡을 PC에 Node.js 24를 설치하고 압축을 풉니다.
2. Windows는 `START_PARTY_WINDOWS.bat`, macOS/Linux는 `sh START_PARTY_MAC_LINUX.sh`를 실행합니다. 최초 실행 때 인터넷으로 npm 의존성을 설치합니다.
3. 실행 창에 출력되는 **같은 Wi-Fi 주소**를 PC와 휴대폰 양쪽에서 엽니다. 예: `http://192.168.0.10:8787/party.html`. 휴대폰에서 `localhost`를 입력하면 PC 서버로 연결되지 않습니다.
4. 계정을 만들거나 로그인한 다음, 첫 접속이라면 중복되지 않는 캐릭터 이름을 정합니다. 한 사람이 인력사무소에서 파티를 만들면 다른 사람은 모집 목록이나 방 코드로 참가합니다.
5. 다른 사람이 참가하고, 두 사람 모두 **준비 완료** → 파티장이 **함께 출격**을 누릅니다.
6. 모바일은 가로 화면으로 플레이합니다. 서버 PC의 실행 창은 열어 둡니다.

방화벽이 접속을 막는 PC는 신뢰하는 개인 네트워크에서 Node.js의 TCP 8787 접속을 허용해야 합니다. 손님 Wi-Fi/기기 격리 설정에서는 기기끼리 연결되지 않을 수 있습니다.

같은 PC로 확인하려면 일반 창과 시크릿 창 또는 서로 다른 브라우저를 사용하세요. 동일 브라우저의 일반 탭 두 개는 같은 캐릭터이므로 두 번째 접속이 첫 번째 접속을 대체합니다.

터미널 실행:

```sh
npm ci --omit=dev --ignore-scripts --no-audit --no-fund
npm run server
```

## 쉘터와 채팅

- 길드를 만들거나 길드 초대 코드로 쉘터에 합류합니다. 소속과 길드 정보는 서버에 저장됩니다.
- 채팅에서 **월드 / 길드 / 파티**를 선택합니다. 길드·파티 채팅은 같은 소속만 받습니다.
- 던전에서는 **채팅** 버튼으로 같은 채널을 이용할 수 있습니다.
- 채팅은 160자, 1초 간격입니다. 최근 메시지는 서버가 켜져 있는 동안만 유지합니다.
- 처음에는 체험 접속으로 시작해도 됩니다. 같은 캐릭터를 다른 기기에서 이어 하려면 **현재 캐릭터를 계정에 저장**에서 계정을 연결하세요.

## 조작

| 동작 | 키보드 | 모바일 |
|---|---|---|
| 이동 | WASD / 방향키 | 왼쪽 스틱 |
| 공격·카운터 | J / Space | 공격 |
| 스매시 | U | 강타 |
| 회피 / 방어 | K / L 유지 | 회피 / 방어 유지 |
| 조준 부위 | Q / Tab | 조준 |
| 기술 / 궁극기 | 1–4 / R | 기술 / 궁극 |
| 조사·정비 | F | 조사 |
| 동료 소생 | 가까이서 V를 3초 유지 | 동료 소생 3초 유지 |

허수아비 3단계, 갈대습지, 정화장에 함께 입장할 수 있습니다. 보스실 진입 전 정비 지점을 이용하세요. 정화장에서는 세 밸브를 잠가야 보스실이 열립니다. 한 명이 먼저 진입하면 살아 있는 파티원 모두 함께 전투를 시작합니다. 쓰러진 동료는 20초 안에 소생할 수 있으며 한 출격에서 각자 최대 두 번 소생합니다. 전멸하면 파티장이 정비 지점에서 재도전합니다.

## 인터넷 공개 서버

현재 패키지는 **공개 서버 주소가 발급된 상태가 아닙니다.** GitHub Pages는 이 Node/WebSocket 서버를 실행하지 않습니다. 모든 기기가 같은 게임 서버의 주소로 접속해야 합니다.

- Docker가 설치된 서버: `docker compose up --build -d`. `docker compose logs -f game`으로 상태를 확인하고 `docker compose down`으로 종료합니다. `down -v`는 저장 볼륨까지 지우므로 사용하지 마세요.
- Render: 저장소에 이 변경본을 업로드한 후 Blueprint에서 `render.yaml`을 선택합니다. **무료 플레이테스트 설정**입니다. 무료 인스턴스에는 영구 디스크를 붙일 수 없어 재시작·재배포·유휴 종료 시 프로필이 초기화되며 게임 화면에도 표시합니다. 장기 운영은 비용 확인 후 영구 저장소가 있는 구성으로 변경해야 합니다. 계정 연결이나 배포는 파일 생성만으로 실행되지 않습니다. 발급된 HTTPS 주소를 파티원에게 공유하면 됩니다.
- 자체 인터넷 서버: 웹 서버의 HTTPS 역방향 프록시에서 HTTP와 WebSocket Upgrade를 동일 출처의 Node 포트로 전달합니다. 단일 프로세스/단일 인스턴스로 운영하세요.

Render 설정 근거: [WebSocket](https://render.com/docs/websocket), [Blueprint 필드](https://render.com/docs/blueprint-spec), [영구 디스크](https://render.com/docs/disks), [무료 인스턴스 제한](https://render.com/docs/free).

## 저장과 현재 범위

- 서버 `.party-data/world.sqlite`에 계정·이름·골드·XP·재료·장비·길드·클리어·거래를 저장합니다. 예전 `profiles.json`은 자동 이전하며 원본은 남깁니다. Docker는 명명된 볼륨을 사용합니다.
- 기본 Render 무료 설정의 `/tmp/hwanghon`은 영구 저장소가 아닙니다. 재시작·재배포·유휴 종료 시 데이터가 초기화됩니다. 장기 운영용 구성은 별도로 준비해야 합니다.
- 백업은 서버를 정상 종료한 뒤 데이터 폴더 전체를 복사하세요. 진행 중인 던전·파티와 채팅 기록은 서버 재시작 시 종료됩니다.
- 이름은 최초 접속 시 확정하며 이후 변경 기능은 없습니다. 계정 비밀번호 재설정·이메일 인증은 아직 없습니다.
- 연결이 끊기면 90초간 기존 파티 복귀를 허용합니다. 동료가 연결되어 있으면 전투가 계속됩니다.
- 협동 보상은 싱글 인벤토리와 분리되어 있습니다. 보급소 구매·착용, 재료 거래가 가능하고 장비의 체력·방어와 XP 성장이 다음 출격에 반영됩니다. 강화·제작·장비 특수 효과 전체는 후속 작업입니다.
- 로컬 100개 접속·25개 동시 보스전과 자동 테스트 70개를 통과했습니다. 실제 인터넷 환경, 장기 부하, 휴대폰 화면·터치 품질까지 검증한 결과는 아닙니다.

최신 범위와 측정값: `docs/design/15-shelter-social-server.md`.
