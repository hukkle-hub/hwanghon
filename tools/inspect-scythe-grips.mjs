import{readFile}from'node:fs/promises';import * as T from '../vendor/three/three.module.js';import{GLTFLoader}from'../vendor/three/GLTFLoader.js';
const b=await readFile('art/3d/ain_scythe_tex.glb'),l=new GLTFLoader();l.register(()=>({name:'no-raster',loadTexture:()=>Promise.resolve(new T.Texture())}));const g=await l.parseAsync(b.buffer.slice(b.byteOffset,b.byteOffset+b.byteLength),'');g.scene.updateMatrixWorld(true);for(const height of [.1,.2,.3,.43,.5,.6,.75,.9,1.1,1.3,1.5]){const values=[],points=[];g.scene.traverse(o=>{if(!o.isMesh)return;const p=o.geometry.attributes.position;for(let i=0;i<p.count;i++){const v=new T.Vector3().fromBufferAttribute(p,i).applyMatrix4(o.matrixWorld);if(Math.abs(v.y-height)<.025){values.push(Math.hypot(v.x,v.z));points.push(v);}}});values.sort((a,b)=>a-b);const box=new T.Box3().setFromPoints(points);console.log(height,{count:values.length,min:values[0],median:values[Math.floor(values.length/2)],max:values.at(-1),box:[box.min.toArray(),box.max.toArray()]});}
